<!-- <section class="products">
    <div class="titleProducts">
        <h2>Productos destacados</h2>    
        <div class="botonesHero">
            <button class="scroll-btn prev-btn"><i class="bi bi-chevron-left"></i></button>
            <button class="scroll-btn next-btn"><i class="bi bi-chevron-right"></i></button>
        </div>
    </div>
        <div class="divProducts1">
            <% productosDestacados.forEach(producto=>{ %>
                <div class="cardProduct">
                    <a href="/products/<%= producto._id %>"><img src="<%= producto.url_imagen[0] %>" alt="imagen del producto"></a>
                    <div class="cardFooter">
                        <div>
                            <h3><%= producto.titulo %></h3>
                            <h3 class="price">$ <%= producto.precio %></h3>    
                        </div>
                        <form class="addToCartForm" action="/products/agregar-carro/<%= producto._id %>" method="POST">
                            <button type="submit" class="bi bi-cart-plus"></button>
                        </form>
                    </div>
                </div>
            <% }) %>
        </div>
</section> -->



<section class="products">
    <div class="titleProducts1">
        <h2>Productos destacados</h2>    
        <div class="botonesHero1">
            <button class="scroll-btn1 prev-btn1"><i class="bi bi-chevron-left"></i></button>
            <button class="scroll-btn1 next-btn1"><i class="bi bi-chevron-right"></i></button>
        </div>
    </div>
    <div class="divProducts1">
        <div class="product-container">
            <% productosDestacados.forEach((producto) => { %>
                <div class="cardProduct1">
                    <a href="/products/<%= producto._id %>">
                        <img src="<%= producto.url_imagen[0] %>" alt="imagen del producto">
                    </a>
                    <div class="cardFooter">
                        <div>
                            <h3><%= producto.titulo %></h3>
                            <h3 class="price">$ <%= producto.precio %></h3>    
                        </div>
                        <form class="addToCartForm" action="/products/agregar-carro/<%= producto._id %>" method="POST">
                            <button type="submit" class="bi bi-cart-plus"></button>
                        </form>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
</section>





<script src="/js/featured.js" type="module"></script>
